import React from 'react';

import styles from './home.scss';

const Home = () => {
  return (
    <div className={styles.home}>
      <p className={styles.pageTitle}>This is home page</p>
    </div>
  );
};

export default Home;
